# spring.application.name=interviewevaluation
spring:
  application:
    name: interviewevaluation
  cloud:
    gcp:
      project-id: google-pub-sub-recruitment-workflow
      pubsub:
        emulator-host: localhost:8085
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: root
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8086